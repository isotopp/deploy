#! /bin/bash

if [ -d venv ]
then
  source venv/bin/activate
else
  exit 1
fi

pip install -U pip
pip install -U wheel

if [ -f requirements.txt ]
then
  pip install -r requirements.txt
fi

if [ -d .git ]
then
  git reset --hard
  git pull --rebase
fi
